[phases.setup]
nixPkgs = ["python3", "python3Packages.pip"]

[phases.install]
cmds = ["python3 -m pip install -r requirements-api.txt"]

[start]
cmd = "python3 -m uvicorn api:app --host 0.0.0.0 --port $PORT"
